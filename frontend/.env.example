# Copy to .env for local development
PORT=3001

# Optional: proxy server URL for signal engine
# Leave empty to use client-side signal calculation (works without proxy)
REACT_APP_PROXY_URL=http://localhost:4000
